import React from 'react'
import { FORM_ELEMENT } from '../../api/GlobalInterface'


export const FormTable = ({form_elements,columns, data}: {form_elements: FORM_ELEMENT[], columns: any,data: any}) => {
  

  return (
    <div>Formtable</div>
  )
}
